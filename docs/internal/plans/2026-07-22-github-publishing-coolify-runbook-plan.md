# GitHub Publishing & Coolify Runbook Implementation Plan

1. Fix recursive Studio package copying and add a rebuild regression test.
2. Add `AGENTS.md`, governance files, CI, issue templates, and PR template.
3. Add GitHub publication scripts and a pre-push secret audit.
4. Add a field-by-field Coolify deployment runbook and environment matrix.
5. Update versioning, manifest, changelog, README, package contents, and JSON bundle.
6. Run typecheck, tests, package inspection, archive verification, and commit the release.
